import {StyleSheet} from "react-native"

const styles = StyleSheet.create({
	icon: {
		width: 30,
		height: 30,
  },
  button: {
    alignItems: 'center',
    padding: 10,
    marginBottom: 30,
  },
})

export default styles